#include "main.h"
#include <stdio.h>
/**
 * main - a program that prints its name, followed by a new line.
 *
 * @argc: the number of commands.
 * @argv: thr commands line arguments.
 *
 * Return: return 0 (success).
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
